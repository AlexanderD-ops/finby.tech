stages:
  - build
  - deploy

build:
  stage: build
  image: node:18
  script:
    - cd landing
    - npm install
    - npm run build
  artifacts:
    paths:
      - build/

deploy:
  stage: deploy
  image: debian:latest
  script:
    - echo "FTP_USER=$FTP_USER"
    - echo "FTP_HOST=$FTP_HOST"
    - apt-get update -qy
    - apt-get install -y lftp
    - ls -la landing/public
    - lftp -d -c "open -u $FTP_USER,$FTP_PASS $FTP_HOST"
  only:
    - dev