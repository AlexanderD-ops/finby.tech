stages:
  - build
  - deploy

build:
  stage: build
  image: node:18
  script:
    - cd landing
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist/

deploy:
  stage: deploy
  image: debian:latest
  script:
    - apt-get update -qy
    - apt-get install -y lftp
    - ls -la landing/public_html
    - lftp -c "open -u $FTP_USER,$FTP_PASS $FTP_HOST; mirror -R ./landing /dev.finby.tech"
  only:
    - dev